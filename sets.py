s= {}#empty dictionary
d = {1,5,32}

e = set()#empty set 
#no element can be repeated in the output even if you have defined it 
#no restriction in data type
d.add(566)
s.remove(1)
# s.pop  randomly removes any
# s.clear() empties the set 
# s1.union(s2) takes the union 
# s1.intersection(s2) takes the intersection 
print(d)
